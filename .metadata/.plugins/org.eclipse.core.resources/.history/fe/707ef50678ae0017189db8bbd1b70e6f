import java.sql.Date;
import java.sql.Time;
import java.time.LocalTime;

public class Calculator {
	  
	public static void main(String[] args) {
		// TODO: setup static data such as fade	
		//TODO: setup splits
        System.out.println("*** Pace caculator - start ***");
        
        Pacechart myChart = new Pacechart();
        myChart.distance = 10;
        myChart.plannedRaceTime =  LocalTime.of(0,59,15);
        myChart.startDelay =  LocalTime.of(0,1,15);
        myChart.fade = 2;
        myChart.createPaceChart();
        
/*        LocalTime test = LocalTime.of(1,48,48);
        System.out.println(utils.TimeToDouble(test));
        System.out.println(utils.DoubleToTime(utils.TimeToDouble(test)));
	*/        		
	    }
	
	// returns the time in minutes and decimal seconds

}
